process {
    cpus = 1
    executor = 'local'

    //errorStrategy = {task.exitStatus == 127 ? 'retry' : 'terminate'}
    maxErrors = '-1'
    maxRetries = 3

    $bwa { cpus = 20 }
}

executor {
    name = 'local'
    queueSize = 10
    cpus = 20
}

